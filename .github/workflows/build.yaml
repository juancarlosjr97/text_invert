---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@a585712be742a18097a5b7f4da156ecf1d665a24 # 0.5.3
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
