---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@de7a9df8d6c0e5d9987dc04f89dc72f5793517fd # 0.7.6
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
