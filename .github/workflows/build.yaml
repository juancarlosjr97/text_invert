---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@3170eda2e69b5a2978220249a67664f81d88c85c # 0.4.4
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
