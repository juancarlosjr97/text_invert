---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@7cfbdb71d84666e20c017ca394e46a10dbead4da # 0.4.6
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
