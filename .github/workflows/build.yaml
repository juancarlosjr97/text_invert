---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@3b755c6717681ce9205b7377183b34687203878f # 0.5.5
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
