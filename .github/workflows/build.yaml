---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@406b2b51db5c9943aef0d489848918a8dce38646 # 0.4.19
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
