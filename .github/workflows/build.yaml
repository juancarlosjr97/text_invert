---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@fad5274e2cffcc6bac606973aed280bafab3aa2c # 0.5.4
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
