---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@2221434c2a903889df1ffd97076824ed30e5fb80 # 0.5.9
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
