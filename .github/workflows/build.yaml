---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@0.4.1
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
