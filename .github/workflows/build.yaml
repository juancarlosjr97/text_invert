---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@c69cafa7aab9f05d504d518b6b2c3e6834c9aa2f # 0.7.11
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
