---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@fe2de3539c151e42df94363a3c20a5a565ab0800 # 0.4.16
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
