---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@a2559e950438719d6af589cfe3d4ca1fa65e87f9 # 0.4.13
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
