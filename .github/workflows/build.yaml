---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@712b7ac309d1b99d69bfd0ca290fbd2d5500fd0c # 0.4.11
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
