---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@2f9dc8afcb6aa89787da571abe7b4d36c7d89d9a # 0.4.9
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
