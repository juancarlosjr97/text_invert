---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@1786e336b3933a065a25618bbf24f4a3db4f6b75 # 0.6.1
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
