---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@948baf0a697a7b8ae6e6e9247c5118905d8af4f8 # 0.5.1
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
