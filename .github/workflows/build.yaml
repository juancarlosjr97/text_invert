---
name: Tests

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-rust-tests.yml@153d479c9eac2ea14bf68fe338eebe054d6bf7ab # 0.5.12
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
